<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunzhi.retailmanagementsystem.Mapper.UsersMapper">

    <resultMap id="BaseResultMap" type="com.yunzhi.retailmanagementsystem.model.domain.Users">
        <id property="userID" column="UserID" jdbcType="VARCHAR"/>
        <result property="username" column="Username" jdbcType="VARCHAR"/>
        <result property="password" column="Password" jdbcType="VARCHAR"/>
        <result property="role" column="Role" jdbcType="VARCHAR"/>
        <result property="approved" column="Approved" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        UserID,Username,Password,Role,Approved
    </sql>

    <!-- 以下是示例查询，可根据需要取消注释并使用 -->
    <!--    <select id="selectByUsername" resultMap="BaseResultMap">-->
    <!--        SELECT <include refid="Base_Column_List"/>-->
    <!--        FROM users-->
    <!--        WHERE Username = #{username}-->
    <!--    </select>-->
    <!--    <select id="selectByUserId" resultMap="BaseResultMap">-->
    <!--        SELECT <include refid="Base_Column_List"/>-->
    <!--        FROM users-->
    <!--        WHERE UserID = #{userId}-->
    <!--    </select>-->
    <!--    <select id="selectByRole" resultMap="BaseResultMap">-->
    <!--        SELECT <include refid="Base_Column_List"/>-->
    <!--        FROM users-->
    <!--        WHERE Role = #{role}-->
    <!--    </select>-->
</mapper>